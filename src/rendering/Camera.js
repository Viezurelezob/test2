export class Camera { constructor(w,h){this.x=0;this.y=0;this.w=w;this.h=h;this.bounds={w,h}} setBounds(w,h){this.bounds={w,h}} follow(target,dt){const tx=target.x+target.w/2-this.w/2,ty=target.y+target.h/2-this.h*.58;this.x+=(tx-this.x)*Math.min(1,dt*5);this.y+=(ty-this.y)*Math.min(1,dt*4);this.x=Math.max(0,Math.min(this.x,this.bounds.w-this.w));this.y=Math.max(0,Math.min(this.y,this.bounds.h-this.h))} }
