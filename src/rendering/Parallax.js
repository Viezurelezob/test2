export function drawParallax(ctx,camera,biome,w,h){const [sky,far,near]=biome.palette;ctx.fillStyle=sky;ctx.fillRect(0,0,w,h);biome.parallax.forEach((layer,i)=>{const y=h*(.28+i*.18),offset=-(camera.x*layer.speed)%220;ctx.fillStyle=i?near:far;for(let x=offset-220;x<w+220;x+=220){ctx.beginPath();ctx.moveTo(x,y+150);ctx.lineTo(x+80,y-(i?80:35));ctx.lineTo(x+170,y+150);ctx.fill()}})}
