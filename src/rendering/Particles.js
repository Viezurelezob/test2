export class ParticlePool { constructor(size=180){this.items=Array.from({length:size},()=>({active:false}))} spawn(x,y,color='#fff',count=8,speed=130){for(let i=0;i<count;i++){const p=this.items.find(v=>!v.active);if(!p)return;const a=Math.random()*Math.PI*2;p.active=true;p.x=x;p.y=y;p.vx=Math.cos(a)*speed*(.3+Math.random());p.vy=Math.sin(a)*speed*(.3+Math.random());p.life=.35+Math.random()*.35;p.max=p.life;p.color=color}} update(dt){for(const p of this.items)if(p.active){p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=180*dt;p.life-=dt;if(p.life<=0)p.active=false}} draw(ctx){for(const p of this.items)if(p.active){ctx.globalAlpha=Math.max(0,p.life/p.max);ctx.fillStyle=p.color;ctx.fillRect(p.x-2,p.y-2,4,4)}ctx.globalAlpha=1} get activeCount(){return this.items.filter(p=>p.active).length} }
